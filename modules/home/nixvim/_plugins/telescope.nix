{
  plugins.telescope = {
    enable = true;
    settings.pickers.find_files = {
      hidden = true;
    };
    settings.defaults = {
      # layout_config = {
      #   prompt_position = "top";
      # };
    };
  };
}
